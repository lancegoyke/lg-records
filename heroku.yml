setup:
    addons:
        - plan: heroku-postgresql
build:
    docker:
        web: Dockerfile
release:
    image: web
    command:
        - python manage.py collectstatic --noinput
run:
    # web: gunicorn records_project.wsgi
    web: /bin/sh -c gunicorn\ records_project.wsgi